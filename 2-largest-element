public static Integer findLargest(List<Integer> nums) {
	//code here
    Integer largestNumber = 0;
    for (Integer currentNum : nums){
        if (currentNum == 0 || currentNum >  largestNumber){
            largestNumber = currentNum;
        } 
    }
    return largestNumber;
}